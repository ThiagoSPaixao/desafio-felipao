//Desafio Classificador de Nível de Herói
//Desafio proposto pelo Prof. Felipão da DIO
//Lá vai...


let nomeDoHeroi = "ThiaguinhoxD";
let classeDoHeroi = "Guerreiro";
let xpDoHeroi = 50000;

if (xpDoHeroi <= 1000) {
    console.log("O Herói " + nomeDoHeroi + " da Classe " + classeDoHeroi + " Está no nível Ferro");
} else if (xpDoHeroi <= 2000) {
    console.log("O Herói " + nomeDoHeroi + " da Classe " + classeDoHeroi + " Está no nível Bronze");
} else if (xpDoHeroi <= 5000) {
    console.log("O Herói " + nomeDoHeroi + " da Classe " + classeDoHeroi + " Está no nível Prata");
} else if (xpDoHeroi <= 7000) {
    console.log("O Herói " + nomeDoHeroi + " da Classe " + classeDoHeroi + " Está no nível Ouro");
} else if (xpDoHeroi <= 8000) {
    console.log("O Herói " + nomeDoHeroi + " da Classe " + classeDoHeroi + " Está no nível Platina");
} else if (xpDoHeroi <= 9000) {
    console.log("O Herói " + nomeDoHeroi + " da Classe " + classeDoHeroi + " Está no nível Ascendente");
} else if (xpDoHeroi <= 10000) {
    console.log("O Herói " + nomeDoHeroi + " da Classe " + classeDoHeroi + " Está no nível Imortal");
} else {
    console.log("O Herói " + nomeDoHeroi + " da Classe " + classeDoHeroi + " Está no nível Radiante");
}










